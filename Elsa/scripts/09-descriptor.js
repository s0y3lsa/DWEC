'use strict';

/*
9-Crea una función que reciba un número variable de parámetros y que los agrupe por tipo.
Después por cada tipo se mostrará el tipo y la colección de parámetros de ese tipo incluyendo
la posición original entre los parámetros.
Añade el código auxiliar necesario para probar la aplicación.
*/

function variableParametro() {

    let variable = '';

    
    do {
        //(typeof variable)
        variable = prompt = ('introduce una variable');

        // Muestra el valor introducido por el usuario
        console.log("El valor introducio es: " + variable);

        // Muestra el tipo de la variable introducida
        console.log("El tipo de la variable es: " + typeof variable);


        if (variable ===  ) {

        }

        continuar = confirm('Desea introducir otra variable?');

    } while (continuar);



};